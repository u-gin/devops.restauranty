apiVersion: v1
kind: Service
metadata:
  name: eugene-service-client
  namespace: eugene
spec:
  selector:
    app: eugene-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP 
